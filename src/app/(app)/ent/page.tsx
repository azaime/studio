import { Card, CardContent, CardDescription, CardHeader, CardTitle } from "@/components/ui/card";

export default function ENTPage() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Service d'Oto-rhino-laryngologie (ORL)</CardTitle>
        <CardDescription>
          Diagnostic et traitement des troubles du nez, de la gorge et des oreilles.
        </CardDescription>
      </CardHeader>
      <CardContent>
        <p>Le contenu de la page ORL sera bientôt disponible.</p>
      </CardContent>
    </Card>
  );
}
