import { Card, CardContent, CardDescription, CardHeader, CardTitle } from "@/components/ui/card";

export default function UltrasoundPage() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Service d'Échographie</CardTitle>
        <CardDescription>
          Gestion des examens d'échographie médicale.
        </CardDescription>
      </CardHeader>
      <CardContent>
        <p>Le contenu de la page Échographie sera bientôt disponible.</p>
      </CardContent>
    </Card>
  );
}
