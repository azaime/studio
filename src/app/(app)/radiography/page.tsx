import { Card, CardContent, CardDescription, CardHeader, CardTitle } from "@/components/ui/card";

export default function RadiographyPage() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Service de Radiographie</CardTitle>
        <CardDescription>
          Gestion des examens d'imagerie par rayons X.
        </CardDescription>
      </CardHeader>
      <CardContent>
        <p>Le contenu de la page Radiographie sera bientôt disponible.</p>
      </CardContent>
    </Card>
  );
}
